# ship — Phase spec

> Definition of done: deployable and backed up

## Subtasks
- [ ] Linux desktop build + packaging (.deb / AppImage)
- [ ] macOS build + notarisation
- [ ] Windows build + installer
- [ ] Daemon installation script / packaging
- [ ] Data backup/restore for cerebrum.db + notes
- [ ] README finalisation per platform
- [ ] License verification (AGPL-3.0)
- [ ] Release CI/CD pipeline

## Notes
- Daemon needs Python + Ollama documented per platform
- SQLite runtime lib needed on Linux
- Offline-first = works immediately after install
